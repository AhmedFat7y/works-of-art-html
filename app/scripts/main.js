'use strict';
$(document).ready(function(){

  /* SVG injection in the body
  $.get('images/svg-defs.svg', function(data) {
    var $icons = $('<div class="icons"></div>').html(new XMLSerializer().serializeToString(data.documentElement));
    $('body').prepend($icons);
  */

  });
});
